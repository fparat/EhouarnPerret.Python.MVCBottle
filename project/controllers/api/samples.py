from project.helpers.argcheck import check_value_type

SAMPLES = []


def get_samples(no: None) -> Sample:
    if isinstance(SAMPLES):

    if not no:
        return tuple(SAMPLES)
    if isinstance(no, int):
        if SAMPLES:

        return ValueError

